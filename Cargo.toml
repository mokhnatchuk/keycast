[package]
name = "keycast"
version = "0.1.0"
edition = "2024"

[dependencies]
evdev = "0.13.2"
arboard = { version = "3.6", default-features = false, features = ["wayland-data-control"] }

[profile.release]
opt-level = 3
lto = true
codegen-units = 1
strip = true
